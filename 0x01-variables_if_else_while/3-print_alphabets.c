#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry Point
 *
 * return: always 0 (success)
 */
int main (void)
{
	char low;

	for(low = 'a'; low <= 'z'; low++)
		putchar(low);
	for(low = 'A'; low <= 'Z'; hi++)
		putchar(low);
		printf('\n');
	return(0);
}
