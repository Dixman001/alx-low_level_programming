#include "main.h"
/**
 * _isupper - checks for upper case
 * @c: a declared character
 * Return: 1 if c is uppercase and 0 if otherwise
 */
int _isupper(int c)
	char c;
	if (c > 'A' && c < 'Z')
		isupper (c);
		return (1);
	else
		return (0);
