#include "main.h"
/**
 * _abs - Entry point
 * Description: Compute absol;ute value of integer
 * @n: AN integer
 * Return:Value returned in n
 */
int _abs(int n)
{
	if (n < 0)
	n = -n;
	return (n);
}
