#include "main.h"
/**
 * _islower - Entry point
 * Description: Check for lowercase character
 * @c: a character to search for
 * Return: value reiurned 0 or 1 as the case may be 
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
